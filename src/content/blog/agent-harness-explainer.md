---
title: "从模型到智能体：Agent、Harness、Hermes Agent 与 openclaw"
description: "沿一条主线拆开 AI Agent 技术栈：Agent 范式、Harness 运行骨架、Hermes Agent 自托管框架、openclaw 个人助手，从技术原理讲到落地应用。"
pubDate: 2026-06-07
category: 未分类
tags: ["AI Agent", "Harness", "Hermes Agent", "openclaw", "自托管", "大模型"]
---

## 目录

01. 执行摘要：一条主线，四个层次
02. Agent：从「会说」到「会做」
03. Harness：智能体的运行骨架
04. Hermes Agent：自我进化的自托管框架
05. openclaw：把骨架装进日常
06. 四层如何咬合：一次完整调用
07. 落地选型与边界
08. 附录：术语表与参考资料

01 · Executive Summary

# 执行摘要：一条主线，四个层次

2025 到 2026 年，大模型真正的变化不是「会说得更好」，而是「能动手做事」。 而让模型从对话框走进真实世界、敢去执行操作的，往往不是模型本身，是包在模型外面的那一层工程。 这份文档沿一条主线，把这层工程拆成四块来讲。

## 核心结论

-   **Agent 是范式**：把无状态的模型放进「感知 — 决策 — 行动 — 观察」的循环，它才从答题者变成执行者。
-   **Harness 是骨架**：负责循环、上下文、工具调度与权限。Claude Code、openclaw 本质都是 harness（智能体运行时）。
-   **Hermes Agent 是自托管框架**：Nous Research 2026 年 2 月开源，模型无关，靠「完成任务即自动写 skill」的闭环学习越用越强，已超 4.6 万 GitHub star。
-   **openclaw 是应用形态**：把智能体从终端搬进聊天软件，2026 年 1 月底发布后一周内拿下 1 万 + GitHub star。

> **本文档回答的问题**
> 
> 一个模型怎么才算成了 Agent？harness 到底替模型干了哪些活？Hermes Agent 凭什么能越用越强？openclaw 又把这套骨架做成了什么样的产品？

四个名词常被混在一起谈，但它们其实落在不同层。把层分清，选型时就不会拿模型的问题去怪框架，也不会指望换个框架解决模型的短板。 下一章先从最上层的概念——「Agent 到底新在哪」——讲起。

02 · The Paradigm

# Agent：从「会说」到「会做」

Agent（智能体）不是一个更大的模型，而是一个被放进闭环里、能自己决定下一步动作的模型。 差别不在参数量，在于它有没有一个「行动 — 观察 — 再行动」的回路。

## 对话与智能体的分界

传统对话是**单轮补全**：给一段输入，模型吐一段文字，结束。它不知道自己说得对不对，也碰不到外部世界。 Agent 把这条直线掰成了环：模型每说一句话，都可能是一个**动作指令**，这个动作会真的被执行，执行结果再喂回模型，让它判断下一步。 模型因此第一次能「看到」自己行为的后果，并据此修正。

## 最小回路：推理与行动交替

这套回路最经典的表述是 ReAct（Reasoning + Acting，让模型把「想一步」和「做一步」交替进行，2022 年由 Yao 等人提出）。 一轮 Agent 循环通常是这样四步：

1.  **感知**：读入任务目标和当前可见的环境状态（已有的对话、文件、上一步工具返回）。
2.  **规划**：模型推理出下一步该调用哪个工具、传什么参数。
3.  **行动**：工具被真正执行——读文件、跑命令、查接口。
4.  **观察**：把执行结果写回上下文，进入下一轮，直到任务收尾。

> 关键的一句：模型自己**不会**执行任何动作。它只会「输出想调用某个工具」这个意图。真正去把命令跑起来、把结果接回来的，是模型外面那层代码。这层就是下一章的主角——harness。

## 为什么模型本身不够

单独的大模型是一个**无状态函数**：输入一段文本，输出一段文本，调用之间不记得任何东西，也没有手脚去碰外部系统。 要把它变成能连续干活的 Agent，至少得有外部补上三样东西：跨轮次的记忆、可调用的工具、以及驱动这一切转起来的循环。 这三样都不在模型权重里，而在它外面的运行层。理解了这一点，整个技术栈的分层就自然浮现出来。

01 应用层 · Application openclaw · Hermes Agent · Claude Code：渠道 · 会话 · 调度 02 · FOCAL 框架层 · Harness 主循环 · 上下文管理 · 工具调度 · 权限 03 协议层 · Protocol 函数调用：tool\_call / tool\_response（通用接口） 04 模型层 · Model LLM 权重：Claude · GPT · Qwen · Hermes 意图 结果 焦点层：让模型变成 Agent 的关键 左：用户意图向下传递　右：执行结果向上回灌*图 1　Agent 技术栈四层。模型负责「想」，协议负责「传」，框架负责「驱动循环」，应用负责「接入生活」。换掉最底层的模型，上面三层基本不动——这正是分层的价值。*

03 · The Runtime

# Harness：智能体的运行骨架

Harness（直译「挽具」，这里指智能体的运行骨架）是把无状态模型变成有状态 Agent 的那层工程。 你日常用的 Claude Code 是一个 harness，本文后面要讲的 openclaw 也是一个 harness——它们形态各异，干的是同一类活。

## harness 替模型扛下的四件事

模型只管「想下一步」，剩下让循环转起来、不转崩的工程，全在 harness 里。它的职责可以收敛成四类：

职责

具体做什么

不做会怎样

**循环控制**

反复调用模型，直到本轮没有新的工具调用为止

模型只能答一次，无法连续完成多步任务

**上下文管理**

把历史、工具结果按窗口大小拼装、压缩、截断

对话一长就超出窗口，请求直接报错

**工具调度**

解析模型给出的调用、执行真实操作、回灌结果

模型说要调工具，却没人真的去执行

**权限与安全**

在不可逆动作前拦一道，要么审批要么沙箱

模型一句话就能删库、发邮件、花钱

## 主循环：整套范式的心脏

把上面四件事压成代码，就是所有 harness 的同一个骨架——一个「调模型 → 执行工具 → 回灌结果」的 while 循环：

```python
# harness 主循环：把无状态模型驱动成有状态 agent
messages = [system_prompt, user_task]
while True:
    response = model.generate(messages, tools=TOOLS)   # 1. 调模型，附上可用工具
    if not response.tool_calls:                          # 2. 没有工具调用 → 任务收尾
        return response.text
    for call in response.tool_calls:                     # 3. 逐个执行模型要求的动作
        result = dispatch(call.name, call.arguments)     #    真正干活、碰外部世界的地方
        messages.append(tool_result(call.id, result))    # 4. 把结果写回上下文
    messages = compact_if_needed(messages)               # 5. 超出窗口就压缩，再进入下一轮
```

这二十行不到的循环，就是「Agent」一词在工程上的全部含义。框架之间的差异——并发几个工具、怎么压缩上下文、权限在第几步拦截——都是在这个骨架上做加法。

## 上下文工程：harness 最难的活

模型的上下文窗口是**有限且按量计费**的。任务一长，历史消息和工具返回会迅速把窗口塞满，因此第 5 步的「压缩」往往是 harness 里最见功力的部分： 哪些旧消息可以丢、哪些要做成摘要保留、检索到的资料放多少。 这件事在业界被称作**上下文工程**（context engineering，围绕「往窗口里放什么」做的取舍），它对 Agent 表现的影响，常常不亚于换一个更强的模型。

> 判断一个产品「是不是真 Agent」，不看它接的模型多大，看它有没有这个主循环、能不能在工具失败后自己重试或换路。 没有循环的，只是把模型答案套了层壳；有循环的，才谈得上自主。

主循环、上下文、工具调度、权限——这套骨架听起来抽象，但真实产品里就是这么搭起来的。 接下来两章用两个 2026 年走红的开源框架做实证：先看重度自托管、会自我进化的 **Hermes Agent**，再看把 agent 搬进聊天软件的 **openclaw**。

04 · The Framework

# Hermes Agent：会自我进化的自托管框架

Hermes Agent 是 Nous Research 于 2026 年 2 月开源的自托管智能体框架。它把上一章那套 harness 骨架做成了产品， 模型无关、数据全留本机，还加上了一件别的框架还没有的东西——让 agent 越用越强的「闭环学习」。

## 是框架，不是那个同名模型

先和「Hermes 大模型」分开：Hermes Agent 是一套**运行框架**，不是一组权重。 据 Nous Research，它没有再多发一个微调模型，而是构建一个能驱动任意大模型的 agent 框架， 自带持久记忆、技能学习、多平台通信与安全代码执行。Hermes 系列模型可以做它的底座，但框架本身刻意做成模型无关——接 Claude、GPT、Gemini 或任意兼容接口都行。

它采用 MIT 许可，发布后累计超过 4.6 万 GitHub star、346 位贡献者，是开源 agent 生态里增长最快的项目之一。

## 三层架构与工具注册表

Hermes Agent 采用分层设计，把**用户界面 / agent 核心逻辑 / 执行后端**三层解耦。读懂它的构件，差不多就读懂了一个成熟 harness 的解剖图：

构件

做什么

**工具注册表**  
`tools/registry.py`

核心枢纽，登记 70+ 工具、约 28 个工具集。每个工具文件在导入时自注册，注册表统一做 schema 收集、分发、可用性检查与错误包装

**执行后端**

同一个工具可落到多种环境：终端就有 6 种后端（本地 / Docker / SSH / Daytona / Modal / Singularity），另有浏览器、Web、MCP 等后端

**会话存储**

基于 SQLite，配 FTS5 全文检索；会话带血缘追踪（压缩前后的父子关系）、按平台隔离、原子写入

**网关进程**

一个长驻进程接 20 个平台适配器，统一会话路由、用户授权、命令分发，以及 hook 与 cron 定时

这套注册表正是上一章主循环里 `dispatch()` 那一步的真身——工具如何登记、参数 schema 怎么收集、执行落到哪个后端，都在这里裁定：

```python
# 每个工具文件在导入时自注册到中央注册表
@register_tool
def run_terminal(cmd: str, backend: str = "local") -> str:
    """在 6 种后端之一执行命令：local/docker/ssh/daytona/modal/singularity"""
    return BACKENDS[backend].run(cmd)

# 注册表统一收集 schema、按名分发、检查可用性、包装错误——
# 主循环只管调 dispatch(name, args)，不关心工具具体在哪台机器上跑
```

## 闭环学习：它凭什么「越用越强」

这是 Hermes Agent 最有辨识度的设计。多数 agent 是无状态的，关掉就忘；它把**学习循环**做成了架构的一等公民： 每完成一个复杂任务，agent 会自动写下一个可复用的 skill 文件，下次遇到类似任务直接调用学过的技能，更快收敛。 再配合跨会话的持久记忆与 FTS5 历史检索，它对上下文的掌握随使用时长累积，而不是每次从零开始。

> 一个能说明问题的演示：Nous Research 的 CEO Jeffrey Quesnelle 让它跨多次迭代会话，自主写完了一部 7.9 万字的小说—— 这种要在几十个独立工作会话之间保持连贯的任务，对任何无状态 agent 来说，在结构上都不可能完成。

## 怎么跑、还能拿来做什么

部署门槛被刻意压低：一条 `curl` 命令自动装好全部依赖，支持 Linux / macOS / WSL2，小到一台 5 美元的 VPS 就能跑，大到 GPU 集群或 serverless 也行；数据全部留在本机，无遥测、无云锁定，因此能直接对接本地守护进程或开发环境，不必走 ngrok 之类的隧道。

它还有一层开发者身份：兼做**训练数据平台**——并行生成成千上万条工具调用轨迹、自动 checkpoint，再以 ShareGPT 等格式导出去微调模型。自动化只是一面，造数据是另一面。

Hermes Agent 把 harness 推到了「重度自托管 + 自我进化」这一端。下一章的 openclaw，代表同一类骨架的另一种取舍：更轻、更贴近日常聊天。

05 · The Application

# openclaw：把骨架装进日常

openclaw 是 harness 的一种完整产品形态：它把智能体从开发者的终端，搬进你每天都在用的聊天软件， 让你像发消息一样把任务交给它。2026 年 1 月底发布后，据社区统计一周内拿下 1 万 + GitHub star，是近年增速最快的开源项目之一。

## 它是什么

据 openclaw 官方文档，它是一个跑在你自己机器上的开源 AI 助手（前身名为 Moltbot / Clawdbot，由 Peter Steinberger 与社区构建）。 几个定调它产品气质的选择：采用 MIT 许可、**本地优先**（记忆和数据以 Markdown 文件存在你的磁盘上）、并通过一套可移植的技能格式让社区扩展能力。 它能代你执行 shell 命令、浏览器自动化、收发邮件、读写日历和文件——也正因如此，它的能力边界和风险都比一个聊天框大得多。

## 网关架构：一个单一真相源

openclaw 的核心是一个自托管的**网关**（Gateway）。它一头连着各种聊天渠道——Telegram、WhatsApp、Slack、Signal、Discord 等， 一头连着一个或多个 AI 编码智能体。网关是会话、路由与渠道连接的单一真相源：谁发来的消息、归属哪个会话、交给哪个 agent，全在这里裁决。 它支持多 agent 路由，按 agent、工作区或发送者隔离会话，互不串扰。

入站 / 出站 路由 · 隔离 读写 .md CHANNELS 聊天渠道 Telegram · Slack GATEWAY · FOCAL openclaw 网关 会话路由 · 心跳调度 AGENT A Claude Code（后台 · worktree） AGENT B Codex（隔离会话） LOCAL STATE 本地记忆 ~/ Markdown 文件 焦点：网关（单一真相源） 外部渠道 蓝线：网关向各 agent 会话路由并隔离*图 2　openclaw 网关架构。所有渠道与 agent 会话都经网关路由，因此会话隔离、心跳唤醒、权限审批可以集中实现，而不必散落到每个聊天平台各做一遍。*

## 心跳调度：自主性的工程来源

openclaw 内置一个**心跳调度器**（heartbeat scheduler），按可配置的间隔定时唤醒自己。 这一笔很关键：它让智能体不必等你开口也能运行——可以定时查邮件、盯任务、跑巡检。 「你问一句、它答一句」是助手；「没人催也会按节奏自己动」才是自主智能体，差别就落在这个调度器上。

## 跑编码智能体：把 Claude Code 挂到聊天里

通过 `openclaw-code-agent` 这类插件，openclaw 能把 Claude Code、Codex 作为**后台编码会话**来管理： 在它们之上加了计划审批、会话生命周期、唤醒路由、worktree 隔离、合并与 PR 跟进。 于是你可以从 Telegram 或 Discord 发起一个编码任务，并在第一条消息之后持续观察它的进展。其默认工作流是 Plan → Review → Execute：先出计划、审过再执行。

```bash
# 官方推荐用安装脚本；若已自备 Node 22+，也可手动安装
npm install -g openclaw@latest
openclaw onboard --install-daemon   # 引导配置并装成后台守护进程
```

## 能力越大，越要先谈安全

> openclaw 连着真实的消息平台，应把所有入站消息当作**不可信输入**看待。官方给出的几条底线值得照搬： 在服务商一侧设硬性消费上限（而非只在 agent 配置里限），任何不可逆动作——支付、删除、发邮件、对外操作——都必须卡一道人工审批， 锁定到近期的安全版本并持续打补丁，除非你清楚网络配置，否则不要把它暴露到公网。

讲到这里，四层都各自亮过相。但它们真正的价值在于**咬合在一起**。下一章用一次完整的请求，把四层从头到尾串一遍。

06 · End to End

# 四层如何咬合：一次完整调用

分层讲清楚了各自的职责，但只有把它们串成一次真实请求，才看得出这套设计的精巧。 设想一个场景：你在手机上发一句「帮我把仓库里失败的测试修好」，看这句话怎么穿过四层、又怎么带着结果回来。

1.  **应用层 · 接入**：你在 Telegram 发出这句话。openclaw 网关收到，按发送者路由到对应会话，选定一个后台的 Claude Code 智能体来接活。
2.  **框架层 · 组装**：harness 把任务、仓库当前状态、可用工具清单组装成上下文，进入主循环，并把每个工具的 schema 一并发给模型。
3.  **模型层 + 协议层 · 决策**：模型推理后，按函数调用格式吐出一个 `<tool_call>`（比如 `run_tests`）。注意——这一步它只给出「意图」，并没有真的跑测试。
4.  **框架层 · 执行回灌**：harness 接住这个调用，真正去执行，把测试输出以 `<tool_response>` 写回上下文。
5.  **循环 · 收敛**：模型读到失败堆栈，再吐下一个调用——改文件、重跑——harness 继续执行回灌。如此往复，直到测试转绿、本轮不再有新的工具调用。
6.  **应用层 · 回送**：网关把最终结果连同一个 PR 发回你的 Telegram；整个过程跑在隔离的 worktree（独立工作副本）里，不会污染主分支。

> 串起来看，每一层只解决自己那一段：模型负责**判断下一步**，协议负责**把判断说清楚**，框架负责**真正执行并驱动循环**，应用负责**接入你的生活并守住权限**。 任意一层被替换——换个更强的模型、换种协议、换个聊天平台——其余三层几乎不用动。这种解耦，正是「分层」二字在这里值钱的地方。

> **一句话记住**
> 
> Agent ＝ 模型（会判断）＋ 协议（说得清）＋ 框架（跑得动）＋ 应用（用得上）。少了任何一层，它要么不会动，要么没法用。

07 · Adoption

# 落地选型与边界

理解了分层，选型就从「跟风挑名字」变成「按约束选层」。先看你的硬约束落在哪一层，再决定每层用什么。

## 按约束选组合

你的首要约束

建议组合

原因

要最强通用能力，可接受闭源

闭源模型接口 + 厂商 SDK，或直接用 Claude Code

省去自建 harness，循环与工具调度已经成熟

要重度自托管、想让 agent 越用越强

Hermes Agent

数据全留本机，闭环学习自动沉淀技能，还能导出训练数据

想从手机 / 聊天软件指挥、做个人自动化

openclaw

网关已接好多渠道，自带心跳调度与多 agent 路由

主要在编辑器和终端里写代码

Claude Code 等终端 harness

贴着代码库工作，工具与权限模型为编码而设计

这四行不互斥。openclaw 把 Claude Code 当后台 agent 来跑，就是「应用层 + 框架层」的组合；Hermes Agent 既能独立当框架，也能被别的网关当后台会话调用。**分层的意义，正是让你按需混搭，而不是被某个产品名绑死。**

## Hermes Agent 还是 openclaw

两者是这波里最像的一对：都自托管、都 MIT、都本地优先、都接一堆聊天平台、都跑一个网关进程。差别在它们各自把取舍推向了哪一端。

维度

Hermes Agent

openclaw

发布 · 热度

2026.02 · 4.6 万 star

2026.01 · 1 万 + star

定位

会自我进化的 agent 框架

装进聊天软件的个人助手

记忆

SQLite + FTS5，自动写可复用 skill

本地 Markdown 文件

最大卖点

闭环学习、训练数据平台

心跳调度、挂现成 coding agent

最适合

长期自托管、要 agent 越用越强

快速从手机指挥日常自动化

## 三条绕不开的边界

-   **自主性与可控性是一对反向量**：心跳越勤、自动执行越多，越要在不可逆动作前加审批闸。把自主性开满又不设闸，等于把方向盘交给一个不会停的循环。
-   **提示注入是新的攻击面**：智能体读取的网页、邮件、消息里可能藏着「忽略以上指令」的诱导。凡是入站内容，默认当不可信处理。
-   **成本会被循环放大**：一个没有上限的主循环可能反复调用模型。务必设预算与步数上限，循环失控时能自动熔断。

> **周一可以做的一件事**
> 
> 挑一个你每周重复在做的小任务，用一个现成 harness 把「Plan → Review → Execute」跑通一轮，并刻意把人工审批卡在最后那个不可逆动作之前。先把闸建好，再谈放权。

08 · Appendix

# 附录：术语表与参考资料

## A. 术语表

**Agent（智能体）**：被放进「感知 — 决策 — 行动 — 观察」循环、能自主决定下一步动作的模型系统。

**Harness（运行骨架）**：包在模型外、负责循环控制、上下文管理、工具调度与权限的运行时。Claude Code、Hermes Agent、openclaw 都是 harness。

**主循环（Agent Loop）**：「调模型 → 执行工具 → 回灌结果」反复进行、直到本轮无新工具调用的循环。

**ReAct**：让模型把「推理一步」和「行动一步」交替进行的智能体范式（Yao 等人，2022）。

**函数调用（Function Calling）**：模型以结构化格式输出「要调用哪个工具、传什么参数」的能力。

**上下文窗口（Context Window）**：模型单次能读入的 token 上限，有限且按量计费。

**上下文工程（Context Engineering）**：围绕「往窗口里放什么、丢什么、压缩什么」做的取舍。

**MCP（Model Context Protocol）**：Anthropic 2024 年提出的标准，规范 harness 怎么连接外部工具与数据。

**Gateway（网关）**：Hermes Agent 与 openclaw 中负责会话路由、渠道连接与授权的长驻进程。

**Heartbeat（心跳调度）**：按固定间隔唤醒智能体、使其无需提示也能运行的调度器。

**Hermes Agent**：Nous Research 2026 年 2 月开源的自托管 agent 框架，模型无关，以「完成任务即自动写可复用 skill」的闭环学习为核心特征。

**闭环学习（Closed Learning Loop）**：agent 完成任务后自动把过程沉淀为可复用技能，使其越用越强。

## B. 参考资料

-   Hermes Agent 官网 — hermes-agent.org
-   Hermes Agent 架构文档 — hermes-agent.nousresearch.com/docs/developer-guide/architecture
-   Nous Research（含 Hermes 模型家族）— nousresearch.com
-   openclaw 仓库 — github.com/openclaw/openclaw
-   openclaw 官方文档 — docs.openclaw.ai

> **阅读说明**
> 
> 文中产品状态、版本与社区数据整理自上述公开来源，截至 2026 年 6 月；这类信息变动较快，落地前请以官方页面为准。